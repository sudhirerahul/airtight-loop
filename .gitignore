# Runtime live-capture data — regenerated by the pollers, not committed
feed/captured/*.ndjson

# Secrets — never commit an API key
.env
*.env

# Java / Maven
engine/target/

# Python
__pycache__/
*.pyc

# OS
.DS_Store
